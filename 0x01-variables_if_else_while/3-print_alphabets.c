#include <stdio.h>
/**
 * main - Entry point
 * description: alphabet in lowercase
 *
 * Return: Always 0
 */
int main(void)
{
	int n = 97;

	int N = 65;

	while (n < 123)
	{
		putchar(n);
		n++;
	}
	while (N<91)
		putchar(N);
		N++;
	}
	putchar('\n');
	return (0);
}
